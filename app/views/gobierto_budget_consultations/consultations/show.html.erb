<div class="column consultation">

  <div class="consultation-intro">
    <div class="consultation-text">
      <%= image_tag 'illustrations/consultation.png', class: 'mt2' %>
      <h2 class="consultation-lead mb3">
        <%= @consultation.title %>
      </h2>
    </div>

    <div class="consultation-step">
      <% if @consultation.open? %>
        <%= link_to [@consultation, :new_response], class: "consultation-link" do %>
          <div class="bottom-info">
            <div class="teaser">
              <h2 class="consultation-q inline-block">¿Quieres opinar?</h2>
              <div class="arrow-right"><i class="fa fa-arrow-right" aria-hidden="true"></i></div>
            </div>

            <p>Es sencillo, y sólo te llevará unos minutos. Entre todos conseguiremos mejorar la eficiencia de nuestro ayuntamiento</p>
          </div>
        <% end %>
        <a class="consultation-deadline" href="/sandbox/consultation-faq.html">Consulta abierta hasta el <%= l(@consultation.closes_on, format: :short) %>.</a>
      <% else %>
        <% if @consultation.upcoming? %>
          <div class="bottom-info">
            <div class="teaser">
              <h2 class="consultation-q inline-block">
                Podrás participar en esta consulta a partir del <%= l(@consultation.opens_on, format: :short) %>
              </h2>
            </div>
          </div>
        <% else %>
          <div class="bottom-info">
            <div class="teaser">
              <h2 class="consultation-q inline-block">
                Lo sentimos, esta consulta está cerrada
              </h2>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
