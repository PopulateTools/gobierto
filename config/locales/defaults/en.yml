---
en:
  activerecord:
    errors:
      messages:
        record_invalid: 'Validation failed: %{errors}'
  country: Spain
  date:
    abbr_day_names:
    - Sun
    - Mon
    - Tue
    - Wed
    - Thu
    - Fri
    - Sat
    abbr_month_names:
    -
    - Jan
    - Feb
    - Mar
    - Apr
    - May
    - Jun
    - Jul
    - Aug
    - Sep
    - Oct
    - Nov
    - Dec
    day_names:
    - Sunday
    - Monday
    - Tuesday
    - Wednesday
    - Thursday
    - Friday
    - Saturday
    formats:
      day_month: "%d/%m"
      default: "%Y-%m-%d"
      long: "%B %d, %Y"
      short: "%b %d %Y"
      shortest: "%d %b %y"
    month_names:
    -
    - January
    - February
    - March
    - April
    - May
    - June
    - July
    - August
    - September
    - October
    - November
    - December
    order:
    - :year
    - :month
    - :day
  datetime:
    distance_in_words:
      about_x_hours:
        one: about 1 hour ago
        other: about %{count} hours ago
      about_x_months:
        one: about 1 month ago
        other: about %{count} months ago
      about_x_years:
        one: about 1 year ago
        other: about %{count} years ago
      almost_x_years:
        one: almost 1 year ago
        other: almost %{count} years ago
      half_a_minute: half a minute ago
      less_than_x_minutes:
        one: less than a minute ago
        other: less than %{count} minutes ago
      less_than_x_seconds:
        one: less than 1 second ago
        other: less than %{count} seconds ago
      over_x_years:
        one: over 1 year ago
        other: over %{count} years ago
      x_days:
        one: 1 day ago
        other: "%{count} days ago"
      x_minutes:
        one: 1 minute ago
        other: "%{count} minutes ago"
      x_months:
        one: 1 month ago
        other: "%{count} months ago"
      x_seconds:
        one: 1 second ago
        other: "%{count} seconds ago"
      x_years:
        one: 1 year ago
        other: "%{count} years ago"
    prompts:
      day: Day
      hour: Hour
      minute: Minute
      month: Month
      second: Seconds
      year: Year
  errors:
    format: "%{attribute} %{message}"
    messages:
      accepted: must be accepted
      array_too_short:
        one: is too short (minimum at least 1 element)
        other: is too short (minimum at least %{count} elements)
      blank: can't be blank
      blank_for_modules: Organization is miss-configured. The identifier is mandatory
        for the modules you have configured.
      collection_taken: can't have another collection of this type.
      confirmation: doesn't match %{attribute}
      empty: can't be empty
      equal_to: must be equal to %{count}
      even: must be even
      exclusion: is reserved
      greater_than: must be greater than %{count}
      greater_than_or_equal_to: must be greater than or equal to %{count}
      inclusion: is not included in the list
      invalid: is invalid
      invalid_consultation_response: Consultation couldn't be saved. Please, review
        the responses and try again
      invalid_table_name: Must start with a letter or an underscore; the rest can
        only contain letters, digits or underscores
      less_than: must be less than %{count}
      less_than_or_equal_to: must be less than or equal to %{count}
      not_a_number: is not a number
      not_an_integer: must be an integer
      not_found: not found
      odd: must be odd
      other_than: must be other than %{count}
      page_as_homepage: The changes to the page could not be saved because You can't
        change the vibility of a page that is used as a homepage.
      present: must be blank
      record_invalid: 'Validation failed: %{errors}'
      restrict_dependent_destroy:
        one: Cannot delete record because a dependent %{record} exists
        other: Cannot delete record because dependent %{record} exist
      taken: has already been taken
      too_long:
        one: is too long (maximum is 1 character)
        other: is too long (maximum is %{count} characters)
      too_short:
        one: is too short (minimum is 1 character)
        other: is too short (minimum is %{count} characters)
      wrong_length:
        one: is the wrong length (should be 1 character)
        other: is the wrong length (should be %{count} characters)
    template:
      body: 'There were problems with the following fields:'
      header:
        one: 1 error prohibited this %{model} from being saved
        other: "%{count} errors prohibited this %{model} from being saved"
  helpers:
    select:
      prompt: Please select
    submit:
      create: Create
      submit: Save
      update: Update
  locales:
    ca: Catalan
    ca_short: CA
    en: English
    en_short: EN
    es: Spanish
    es_short: ES
  number:
    currency:
      format:
        delimiter: ","
        format: "%n%u"
        precision: 2
        separator: "."
        significant: false
        strip_insignificant_zeros: false
        unit: "€"
    format:
      delimiter: ","
      precision: 3
      separator: "."
      significant: false
      strip_insignificant_zeros: false
    human:
      decimal_units:
        format: "%n %u"
        units:
          billion: Billion
          million: Million
          quadrillion: Quadrillion
          thousand: Thousand
          trillion: Trillion
          unit: ''
      format:
        delimiter: ''
        precision: 3
        significant: true
        strip_insignificant_zeros: true
      storage_units:
        format: "%n %u"
        units:
          byte:
            one: Byte
            other: Bytes
          gb: GB
          kb: KB
          mb: MB
          tb: TB
    percentage:
      format:
        delimiter: ''
        format: "%n%"
    precision:
      format:
        delimiter: ''
  support:
    array:
      last_word_connector: ", and "
      two_words_connector: " and "
      words_connector: ", "
  time:
    am: am
    formats:
      default: "%a, %d %b %Y %H:%M:%S %z"
      from_to: From %{starts} to %{ends}
      long: "%B %d, %Y %H:%M"
      short: "%d %b %H:%M"
    pm: pm
