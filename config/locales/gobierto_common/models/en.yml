---
en:
  activerecord:
    attributes:
      gobierto_admin/api_token:
        domain: Domain
        name: Name
      gobierto_common/api_token_form:
        domain: Domain
        name: Name
      user/api_token:
        domain: Domain
        name: Name
    errors:
      models:
        gobierto_admin/api_token:
          attributes:
            domain:
              not_valid_format: Invalid format
        user/api_token:
          attributes:
            domain:
              not_valid_format: Invalid format
