<% @events.each do |event| %>
  <%= render partial: "gobierto_people/person_events/person_event", locals: { person: event.collection.container, person_event: event } %>
<% end %>

<%# Disable for performance reasons %>
<%# link_to_next_page @events, t('gobierto_people.person_events.index.view_more'), class: "see_more", role: "button", remote: true %>

