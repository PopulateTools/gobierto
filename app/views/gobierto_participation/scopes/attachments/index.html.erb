<div class='gradient-bg'>
  <div class='column'>
    <div class='pure-g'>
      <div class='pure-u-1 pure-u-lg-1-3'>
        <div class='content_side'>
          <h2 class='with_separator'>
            <%= t('gobierto_participation.shared.documents_for', container: @scope.name) %>
          </h2>
          <% pending do %>
            <%= render 'gobierto_participation/shared/attachments/attachments_filter', { scopes: @scopes } %>
          <% end %>
        </div>
      </div>

      <div class='pure-u-1 pure-u-lg-2-3'>
        <% if @attachments && @attachments.any? %>
          <% @attachments.each do |attachment| %>
            <%= render 'gobierto_participation/shared/attachments/attachment_teaser', { attachment: attachment, scope: @scope } %>
          <% end %>
        <% else %>
          <%= t('gobierto_participation.shared.no_documents') %>
        <% end %>
      </div>
    </div>
  </div>
</div>
