<% title(@page.title) %>
<% description(truncate(strip_tags(@page.body), length: 100)) %>

<% content_for :breadcrumb_current_item do %>
  <strong>
    <%= @collection.title %>
  </strong>
<% end %>

<%= render "gobierto_cms/templates/#{@page.template}", { page: @page } %>
