<%= render partial: 'gobierto_people/person_events/person_event', collection: @events, locals: {person: @person} %>

<%# Disable for performance reasons %>
<%# link_to_next_page @events, t('gobierto_people.people.person_events.index.view_more'), class: "see_more", role: "button", remote: true %>

