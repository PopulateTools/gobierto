demo.gobierto.test:
  ldap_username: uid=admin,ou=people,dc=example,dc=com
  ldap_password: wadus
  configurations:
    -
      host: 127.0.0.1
      port: 389
      domain: ou=people,dc=example,dc=com
      authentication_query: uid=%{user_identifier},ou=people,dc=example,dc=com
      email_field: mail
      password_field: userPassword
      name_field: cn
